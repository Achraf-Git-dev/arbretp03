#ifndef CHAINE_CARACTERE_HH
#define CHAINE_CARACTERE_HH

#include <stdio.h>

int taillemot(char* mot);
char* copie(char* source, int i);
char Minuscule(char* lettre);
char Majuscule(char* lettre);

#endif